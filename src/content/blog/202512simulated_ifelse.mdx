---
title: "CSSカスタムプロパティと数学関数を用いて擬似的な条件分岐を行う"
description: "CSSで擬似的な条件分岐を行う方法"
pubDate: "2025/12/01"
tags: ["Web"]
draft: false
---

CSSで値が特定の値より高いか低いか判定し、その条件ごとに異なる式を使って計算しようとすると、通常はSASSの `@if, @else` に頼るしかない。例えば入力値 `x` が基準値 `y` より大きい場合は `x/y` 、小さい場合は `y/x` の式を用いて値を算出したいケースなど。

これをカスタムプロパティのみで完結したいと考えた。数学に強いエンジニアに相談したところ、「ステップ関数（のようなもの）を作り、式を擬似的に分岐させる」という面白い案が出た。

## 条件ごとに返す値が変わる式を用意する
まずは入力値 `--x` が基準値 `--y` より大きいか小さいかを判定する。
これを行うために、CSS数学関数の `sign()` と `abs()` を用いて三つの式を作る。

```scss
--x: 10; // 入力値
--y: 16; // 基準値

--step-left: clamp(0, -1 * sign(var(--x) - var(--y)), 1);
--step-middle: clamp(0, 1 - abs(sign(var(--x) - var(--y))), 1);
--step-right: clamp(0, sign(var(--x) - var(--y)), 1);
```

`--step-left, middle, right` はそれぞれ `--x` が `--y` より大きいか小さいかに応じて `1` または `0` を返す。

- `x<y` の時、
	- `--step-left = 1`
	- `--step-middle = 0`
	- `--step-right = 0`
- `x=y` の時
	- `--step-left = 0`
	- `--step-middle = 1`
	- `--step-right = 0`
- `x>y` の時
	- `--step-left = 0`
	- `--step-middle = 0`
	- `--step-right = 1`

各関数の返す1をtrue、0をfalseとすることで条件分岐を行える。

## 条件ごとに違う式を用いて計算を行う

この擬似ステップ関数を利用して大小を判別し、`x<y` では `x/y`, `x=y` では `y`, `x>y` では `y/x`の式で、1未満で比率を算出する。

```scss
--x: 10; // 入力値
--y: 16; // 基準値

--step-left: clamp(0, -1 * sign(var(--x) - var(--y)), 1);
--step-middle: clamp(0, 1 - abs(sign(var(--x) - var(--y))), 1);
--step-right: clamp(0, sign(var(--x) - var(--y)), 1);

--ratio: calc(
  // x<y以外なら--step-leftは0なのでこの式は0になる
	(var(--x) / var(--y)) * var(--step-left) + 
  // x=yなら1、そうでないなら0
	var(--step-middle) + 
  // x>y以外なら--step-leftは0なのでこの式は0になる
	(var(--y) / var(--x)) * var(--step-right) 
);
```
`(var(--x) / var(--y)) * var(--step-left)` と `(var(--y) / var(--x)) * var(--step-right)` 二つの式を用意し、`--step-hoge` が返す値が0なら、`*0` されることでその式が使われないことになる。これにより擬似的条件分岐で有効な式を選択させ、`x<y` では `x/y`、`x>y` では `y/x`の式が使われる。

## 使用例

本文のフォントサイズと比べて、対象のフォントサイズが大きいほど、または小さいほど `line-height` を小さくするという処理を行う。

自作フレームワークではこの処理をSassの条件分岐を使って行なっていたが、CSSのカスタムプロパティを使うことで、より動的な処理が可能になる。

### 実際のコード
``` scss

```

これを本文以外のフォントサイズごと個別に行う。本当は処理を関数化し、入力される値は引数を使ってコードをコンパクトにしたかったが、CSSには現在カスタム関数は存在しないため、複数必要な場合はこれらの処理を一文で書く必要がある。一文にすると以下。これがフォントサイズのバリエーションごとに書かれることになる。

```css
--ratio: calc((var(--x) / var(--y)) * clamp(0, -1 * sign(var(--x) - var(--y)), 1) + clamp(0, 1 - abs(sign(var(--x) - var(--y))), 1) + (var(--y) / var(--x)) * clamp(0, sign(var(--x) - var(--y)), 1));
```

普通に動くが、とても保守に向いた形ではないことがわかる。CSSカスタムプロパティとCSS数学関数によって複雑な計算処理が可能になったことで、処理一つ一つをカプセル化して引数ごとに再利用可能になることが求められる。カスタム関数の機能がCSSに追加されれば、より便利になるだろう。

### 関数で書けるなら…
現在仕様提案されている `CSS function` の構文で書けるなら以下のようにできそう。

```scss

```

関数が存在すればここまでコンパクトにできるだろう。SassのMixinに相当する機能があれば、フォントサイズごとに `line-height` 用のカスタムプロパティを用意しなくて良い形になる未来もあるかもしれない。

ともあれ、数年前は想像もできなかった複雑な処理を、CSSのみで行うことが可能になった。昨今の進化が凄まじいCSS、機能が増えればそれだけデザインの幅も広がる。今提案されている新機能を含めて今後がとっても楽しみだ。