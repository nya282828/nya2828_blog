---
import type { ImageMetadata } from 'astro';
import Media from './Media.astro';

type Props = {
  src: string | ImageMetadata;
  alt: string;
};

const { src, alt } = Astro.props;
---
<Media type="img" {src} {alt} />
